<div id="layout_login">
<center><h1>Novo Pedido</h1></center>
<%= form_for(@purchase) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %>Este pedido nao pode ser salvo!</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="margin-left:5px;">
	<%= f.label :Produto %>
        <%= text_field_tag :produto, params[:produto] %>
	
  </div>

  <div class="field" style="margin-left:5px;">
	<%= f.label :Data_Pedido %>
        <%= text_field_tag :data_pedido, params[:data_pedido] %>
  </div>
  
  <div class="field" style="margin-left:5px;">
	<%= f.label :Tempo_estimado %>
        <%= text_field_tag :tempo_estimado, params[:tempo_estimado] %>
  </div>

 <div class="field" style="margin-left:5px;">
	<%= f.label :Valor_da_Entrega %>
        <%= text_field_tag :valor_entrega, params[:valor_entrega] %>
  </div>

 <div class="field" style="margin-left:5px;">
	<%= f.label :Valor_Produtos %>
        <%= text_field_tag :valor_produtos, params[:valor_produtos] %>
  </div>
 
 <div class="field" style="margin-left:5px;">
	<%= f.label :Valor_Total %>
        <%= text_field_tag :valor_total, params[:valor_total] %>
  </div>


  <div class="actions" style="float:left">
	<%= f.submit %>
  </div>
 
<% end %>
</div>
